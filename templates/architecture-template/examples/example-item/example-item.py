"""{Item Name} pattern in {Language}.

{One short paragraph: what scenario this example models (pick something
concrete and domain-flavored, e.g. "a vending machine", "an order
pipeline"), and one sentence on why this is the idiomatic shape for this
language rather than a literal textbook translation.}

Delete this file and its sibling language files if the skill doesn't need
runnable code examples. Otherwise, duplicate this file per language your
skill supports, keeping the filename pattern `<item>.<ext>` and updating
the docstring/comment to match that language's idiom.
"""


def main() -> None:
    # Replace with a minimal, runnable illustration of the item above.
    # Keep it short enough to read in one screen, and prefer real method
    # names over `foo`/`bar` so it reads as a plausible piece of code.
    pass


if __name__ == "__main__":
    main()
